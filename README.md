# capability-specs

一个文件夹一个能力，用 Spec 组织可复用的 AI 问题解决单元。

## 目标
在 AGI 时代到来前，把经典工具能力沉淀为可复用 Spec，帮助 AI 按场景快速调用工具链并解决真实问题。

## 结构约定
- `specs/<capability>/`：一个能力一个文件夹
- 每个能力目录建议包含：
  - `spec.yaml`（能力定义）
  - `README.md`（使用说明）
  - `examples/`（输入输出样例）

## 当前状态
仓库初始化完成，后续将持续在真实场景中迭代。