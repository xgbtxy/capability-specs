# 实战记录

用真实镜像处理经验沉淀“虚拟机镜像静态处理”能力组，而不是只写概念流程。

## 当前验证状态

- `01-识别镜像类型并确认可操作路径`
  需要补充一条真实样本，把“拿到文件后先识别”的过程完整记下来
- `02-离线从虚拟机镜像提取文件`
  已有实际使用基础，但当前仓库里还没写成逐步复盘
- `03-转换虚拟机镜像格式`
  已有实际使用基础，例如围绕 `qemu-img` 的格式转换
- `04-Linux离线恢复登录访问`
  当前作为可执行 Spec 落地，待后续拿真实 Linux 镜像验证

## 建议下一次补记录时至少写清楚

- 输入镜像是什么格式
- 当时用到的命令是什么
- 成功提取了哪些文件，或转换成了什么格式
- 如果失败，卡在识别、挂载还是转换
- Linux 离线恢复访问到底修改了哪些文件、是否启动验证成功

## 记录原则

- 只写自己真实做过的镜像处理
- 已做过但细节不全的，标成“已有使用基础”
- 没跑过的明确写“待验证”
