# Tools and Safety

## 工具

- PowerShell
- Windows 原生命令

## 默认做法

- 先确认是不是 PowerShell 语境
- 先把复杂命令拆成几步
- 先验证路径和输入存在
- 涉及原生命令时同时看输出和退出码

## 注意

- 不要把 `cmd` 语法直接抄进 PowerShell
- 不要在一条长命令里同时做搜索、删除、移动和提交
- 涉及删除、覆盖、重命名时，先做只读确认
