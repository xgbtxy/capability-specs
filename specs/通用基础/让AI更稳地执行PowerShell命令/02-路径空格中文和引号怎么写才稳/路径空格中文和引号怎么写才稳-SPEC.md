# 路径空格中文和引号怎么写才稳

## 目标

优先避免这些问题：

- 路径里有空格
- 路径里有中文
- 路径被拼接后没有正确引用

## 默认做法

- 优先用绝对路径
- 优先单引号包住完整路径
- 一条命令太长时，先拆变量，再执行命令

## 示例思路

```powershell
$repo = 'C:\Users\15412\Desktop\工具包\capability-specs'
Test-Path $repo
```
