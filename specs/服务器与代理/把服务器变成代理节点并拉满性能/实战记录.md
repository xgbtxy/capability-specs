# 实战记录

已验证过一次真实场景：

- Debian 12 小 VPS
- 目标是只保留 `xray` 作为代理节点
- 用 `systemd` 管理服务
- 清掉了一批失败或无关的云残留服务
- 收敛了 `xray` 的运行参数和 DNS 路径
- 发现原端口表面可达，但客户端实测仍有问题，后续切到了更冷门的新端口

这次实战里最值钱的经验：

- 先验证端口可达性，再决定要不要换端口
- 代理节点不是参数越激进越好，`FIFO 99` 这类设置未必带来真实收益
- 把无关服务和日志开销收下来，通常比乱调一堆内核参数更实在
