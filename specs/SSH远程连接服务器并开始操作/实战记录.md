# 实战记录

已验证过一次真实场景：

- 非默认 SSH 端口登录 Linux 服务器
- 用 `Plink` 在 Windows 上完成带密码连接
- 登录后确认 `systemd` 服务、监听端口、内存和失败单元
- 改配置前把关键文件备份到时间戳目录

这次实战里比较值得记住的点：

- Windows 上如果要自动化输入密码，`Plink` 往往比原生 `ssh` 更顺
- 先确认主机指纹，再做脚本化连接
- 真正有价值的第一步不是“立刻优化”，而是先做基线和备份
