# tools-and-safety

## 常用工具

- `xray x25519`
- `v2rayN`
- `Xray`
- `Clash Verge`
- `Mihomo`

## 安全边界

- 先确认服务端真实参数，再生成客户端配置
- 桌面端如果开 `TUN`，要把 `TUN` 路径单独当成问题排
- 手机能用，不代表 Windows 的 `TUN` 一定也能用
- 不要直接覆盖当前保命节点，先单独做测试配置
